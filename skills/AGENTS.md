# Language Guidelines

- All files must be written in English by default.
- If a file already contains content written in a non-English language (e.g., Japanese), respect and continue using that language within the file.
